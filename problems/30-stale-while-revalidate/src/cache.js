// YOUR CODE — a tiny stale-while-revalidate cache.
// read(key) returns { data, stale } synchronously when cached, else null,
// and kicks off a refresh when the entry is stale.
export function createCache({ fetcher, freshMs, onChange }) {
  throw new Error("createCache is not implemented");
}
