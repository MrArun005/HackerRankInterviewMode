// YOUR CODE — retry with exponential backoff.
// Returns the value, or throws the last error after MAX_TRIES attempts.
export function withRetry(fn, { onAttempt } = {}) {
  throw new Error("withRetry is not implemented");
}
