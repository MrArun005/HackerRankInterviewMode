// YOUR CODE — pure.
// Within a facet the choices are OR'd; across facets they are AND'ed.
export function applyFilters(people, { depts, sites, contractOnly }) {
  throw new Error("applyFilters is not implemented");
}
