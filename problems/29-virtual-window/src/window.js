// YOUR CODE — pure. Which rows should be mounted, and where does the slab sit?
export function windowFor({ total, rowHeight, viewportHeight, scrollTop, overscan = 2 }) {
  throw new Error("windowFor is not implemented");
}
