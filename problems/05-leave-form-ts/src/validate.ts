// YOUR CODE — pure validation. Return one message per invalid field.
import type { FieldErrors, LeaveRequest } from "./types";

export function validate(req: LeaveRequest): FieldErrors {
  throw new Error("validate is not implemented");
}
